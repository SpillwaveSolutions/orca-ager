---
name: orca-emit
description: Alias for /orca-compile — emit Orca config + prompts + orchestration script from an AGER graph.
---

Follow the **ager-to-orca** skill. Run `scripts/emit.py`. Same as `/orca-compile`.
